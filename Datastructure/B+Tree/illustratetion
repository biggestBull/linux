花了我三天时间才写出来，本来以为就是麻烦一点，，，后来发现是真的挺麻烦的，，，每次满心欢喜的以为“这下肯定解决了”。
————总会有新的bug出现，当然，所有的一切其实都应该归结于粗心。

一个文件，用来存放b+树。
该文件保留前30个字节，用来保存实际数据的size以及Root的地址。
树中每个节点的指针都是在该文件中的SEEK_SET的偏移地址，实际上我就是把磁盘当作内存来分配。

关于B+树，我认为，需要明白这个概念：B+树是自底向上构造出来的，也是自底向上删除的。（当然，搜素肯定是自顶向下，在内存中保存Root节点）
